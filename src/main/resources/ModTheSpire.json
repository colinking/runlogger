{
  "modid": "${modID}",
  "name": "${project.name}",
  "author_list": ["${author}"],
  "description": "!(project.description)",
  "version": "${project.version}",
  "sts_version": "${SlayTheSpire.version}",
  "mts_version": "${ModTheSpire.version}",
  "dependencies": ["basemod"],
  "optional_dependencies": ["CommunicationMod"]
}